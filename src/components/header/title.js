import React, { Component } from 'react';
import { Link } from 'react-router-dom';

import './title.scss';

const Title = props => {
        return (
            <div className="Title">
                Title
            </div>
        );
}

export default Title;